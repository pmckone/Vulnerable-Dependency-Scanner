# Vulnerable-Dependency-Scanner
Scans project dependencies for known CVEs using the OSV.dev database. Fails CI/CD builds automatically on HIGH or CRITICAL severity findings to prevent vulnerable packages from reaching production.
